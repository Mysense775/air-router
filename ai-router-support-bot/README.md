# AI Router Support Bot 🤖

Telegram бот для технической поддержки клиентов AI Router Platform.

## Возможности

- 🆕 Создание заявок в поддержку
- 📋 Просмотр статуса заявок
- 💰 Проверка баланса
- 📊 История API запросов
- 📸 Прикрепление скриншотов к заявкам
- 🔔 Уведомления операторам в группу

## Быстрый старт

### 1. Настройка бота

```bash
# Перейти в директорию проекта
cd /root/.openclaw/workspace/ai-router-platform

# Запустить бота
./start-support-bot.sh
```

### 2. Настройка группы поддержки

1. Создайте группу в Telegram
2. Добавьте бота @ai_router_support_bot в группу
3. Сделайте бота администратором (для отправки уведомлений)
4. Получите ID группы:
   - Отправьте любое сообщение в группу
   - Перейдите по ссылке: https://api.telegram.org/bot8549699847:AAE_R4pJyqwDXHfudJPJktDmbWB4GFMhOts/getUpdates
   - Найдите "chat":{"id":-100xxxxxxxxxx} — это ID группы

### 3. Настройка окружения

```bash
# Создать .env файл
cat > .env << EOF
SUPPORT_GROUP_ID=-100xxxxxxxxxx
EOF

# Перезапустить бота
docker compose -f docker-compose.support-bot.yml restart
```

## Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Авторизация, начало работы |
| `/new_ticket` | Создать заявку |
| `/tickets` | Мои заявки |
| `/balance` | Проверить баланс |
| `/history` | История запросов |
| `/help` | Помощь |

## Flow работы клиента

1. **Авторизация**: Клиент отправляет API ключ
2. **Создание заявки**: Выбор категории → Описание → Скриншоты → Подтверждение
3. **Уведомление**: Операторы получают уведомление в группу
4. **Ответ**: Оператор отвечает → Клиент получает ответ в бота

## Для операторов

В группе поддержки доступны кнопки:
- ✏️ Ответить — ответить клиенту
- 🔧 В работу — взять заявку в работу
- ✅ Закрыть — пометить как решенную

## Логи

```bash
# Логи бота
docker logs -f airouter-support-bot

# Перезапуск
docker compose -f docker-compose.support-bot.yml restart
```

## Структура проекта

```
ai-router-support-bot/
├── bot/
│   └── main.py          # Основной код бота
├── api/
│   └── client.py        # Клиент для AI Router API
├── models/
│   └── database.py      # SQLAlchemy модели
├── config/
│   └── settings.py      # Конфигурация
├── Dockerfile
└── requirements.txt
```

## TODO

- [ ] Сохранение заявок в БД
- [ ] Webhook для получения обновлений
- [ ] Автоответы на частые вопросы (GPT)
- [ ] Метрики и аналитика
- [ ] Интеграция с внешними системами (Intercom, Zendesk)
